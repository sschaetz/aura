#ifndef AURA_DEVICE_HPP
#define AURA_DEVICE_HPP

namespace aura {

// creates and owns a device context
// owns a default stream
// creates new streams on device
// copy semantics are still unclear
// is an execution environment
class device {


};

} // arua

#endif // AURA_DEVICE_HPP

